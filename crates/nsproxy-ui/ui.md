
reusable structures and functions that situate on the link from GUI/TUI to nsproxy containers, should be placed in `crates/nsproxy-core/src/orchestrate.rs`

the GUI is intended to duplicate part of the CLI binary's code, but for each container there will be individual processes. 
