You need `dom.securecontext.allowlist`. Tap "+" with type string to have vdns mapped domains function properly.

This whitelists a whole domain regardless of ports when using `example.ns`, while this domain may be kept in TUN VDNS. It doesnt work with wildcards.